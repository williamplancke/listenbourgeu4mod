namespace = flavor_csr
normal_or_historical_nations = yes

country_event = {
    id = flavor_csr.1
    title = flavor_csr.1.name
    desc = flavor_csr.1.desc
    picture = MILITARY_CAMP_eventPicture
    is_triggered_only = yes
    fire_only_once = yes
    trigger = {
        tag = CSR
    }
    option = {
        ai_chance = {factor = 50}
        name = flavor_csr.1.a
        declare_war_with_cb = {
            who = LST
            casus_belli = cb_independence_war
        }
    }
    option = {
        ai_chance = {factor = 1}
        name = flavor_csr.1.b
        add_prestige = -10
    }
}