lst_missions_iberian_dominance = {
    slot = 1
    generic = no
    ai = yes
    potential = {
        tag = LST
        NOT = { map_setup = map_setup_random }
    }
    has_country_shield = yes
    lst_rally_the_forces = {
        #icon
        trigger = {
            army_size_percentage = 1
            num_of_generals = 1
        }
        effect = {
            galicia_area = {
                limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
                add_permanent_claim = ROOT
            }
            beieras_area = {
                limit = {
					NOT = { is_core = ROOT }
					NOT = { is_permanent_claim = ROOT }
				}
                add_permanent_claim = ROOT
            }
            define_advisor = {
                type = commandant
                skill = 1
                cost_multiplier = 0.75
            }
        }
    }
}
lst_missions_empty1 = {
    slot = 2
    generic = no
    ai = yes
    potential = {
        tag = LST
        NOT = { map_setup = map_setup_random }
    }
    has_country_shield = yes
}
lst_missions_civil_war = {
    slot = 3
    generic = no
    ai = yes
    potential = {
        tag = LST
        NOT = { map_setup = map_setup_random }
    }
    has_country_shield = yes
    lst_restore_order = {
        #icon
        trigger = {
            has_country_flag = lst_resolved_civil_war
            stability = 1
        }
        effect = {
            change_adm = 1
            add_country_modifier = {
                name = "lst_regained_control"
                duration = 10950 # 30 years
            }
        }
    }
}
lst_missions_empty2 = {
    slot = 4
    generic = no
    ai = yes
    potential = {
        tag = LST
        NOT = { map_setup = map_setup_random }
    }
    has_country_shield = yes
}
lst_missions_economy = {
    slot = 5
    generic = no
    ai = yes
    potential = {
        tag = LST
        NOT = { map_setup = map_setup_random }
    }
    has_country_shield = yes
}